server:
  port: 8080
  tomcat:
    maxThreads: 500
    basedir: .
    accesslog:
      enabled: true
      pattern: "%t %a '%r' %s (%D ms)"

---
spring:
  profiles: dev

---
spring:
  profiles: test

---
spring:
  profiles: prod
